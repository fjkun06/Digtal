import React from 'react'

const FooterContactLink = ({children}) => {
  return (
    <span className='footer__link-contact footer__link'>
      {children}
    </span>
  )
}

export default FooterContactLink
